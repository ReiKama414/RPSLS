{
  "private": true,
  "scripts": {
    "build": "ts-node nuxt.config.ts && npx nuxt build",
    "start": "ts-node nuxt.config.ts && npx nuxt start",
    "dev": "ts-node nuxt.config.ts && npx nuxt dev",
    "generate": "ts-node nuxt.config.ts && npx nuxt generate",
    "preview": "ts-node nuxt.config.ts && npx nuxt preview",
    "postinstall": "ts-node nuxt.config.ts && npx nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/types": "^2.16.0",
    "@nuxt/typescript-build": "^2.1.0",
    "@nuxtjs/tailwindcss": "^6.3.1",
    "nuxt": "^3.1.2",
    "sass-loader": "^13.2.0"
  },
  "dependencies": {
    "sass": "^1.58.0",
    "ts-node": "^10.9.1"
  }
}
