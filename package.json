{
  "private": true,
  "scripts": {
    "build": "nuxt-ts build",
    "dev": "nuxt-ts dev",
    "generate": "nuxt-ts generate",
    "preview": "nuxt-ts preview",
    "postinstall": "nuxt-ts prepare"
  },
  "devDependencies": {
    "@nuxt/typescript-build": "^2.1.0",
    "@nuxtjs/tailwindcss": "^6.3.1",
    "nuxt": "^3.1.2",
    "sass-loader": "^13.2.0"
  },
  "dependencies": {
    "sass": "^1.58.0"
  }
}
